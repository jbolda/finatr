---
"web": minor
"local-sync-server": minor
---

Add support for a local sync server, including plan-page sync controls, and management endpoints for inspection and broadcast. Includes a `/plan` page in the frontend to begin supporting these connections. It is still early, and, as such, not exposed in the sidebar. This includes tests with end-to-end coverage for multi-client sync behavior.
